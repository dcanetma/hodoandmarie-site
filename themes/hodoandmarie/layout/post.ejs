<% if (page.textColor){ %> 
  <style type="text/css">
  	@media (min-width: 992px) {
	    section.post-full  header .intro h1,
	    section.post-full  header .intro h1 a,
	    section.post-full  header .intro h2.post-preview-subtitle,
	    section.post-full  header .intro p,
	    section.post-full  header .intro p a {
	    		color: <%= page.textColor %> !important ; 
	    }
	    section.post-full a.btn {
				border-color: <%= page.textColor %>;
				color: <%= page.textColor %>;
			}
		section.post-full a.btn:hover {
				<% if (page.textColor!='white' && page.textColor!='#fff' && page.textColor!='#ffffff'){ %>
					color: white;
				<% } else { %>
					color: black;		
				<% } %>
				background-color: <%= page.textColor %>;
				border-color: <%= page.textColor %>;
	    }
	  }
  </style>
<% } %> 

<section id="<%= page.layout %>" class="post post-full <%- page.vimeoId > 1 ? 'post-full-video' : 'post-full-no-video' %> container-fluid mt-1 mt-lg-2" >
	<!-- Post header -->
  <header class="row">  
		<div class="col-12 inner">
			 <% if (page.vimeoId > 1){ %>	
				 <img src="<%= url_for(page.imageUrl) %>" alt="<%= page.title %>"  class="header embed-thumbnail img-fluid">  
				 <div class="embed-responsive embed-responsive-16by9">
					 	<iframe id="video" class="header embed-responsive-item" 
					 		src="https://player.vimeo.com/video/<%= page.vimeoId %>" width="1280" height="720" frameborder="0" allowfullscreen allow="autoplay; encrypted-media">
					 		<p>Your browser doesn't support Vimeo Videos. 
					 				<a href="https://vimeo.com/<%= page.vimeoId %>">
					 					Watch this video on Vimeo
					 				</a>.
					 		</p>
					 	</iframe>
					</div>
			 <% } else if (page.vimeoId = 1) { %>
				<img src="<%= url_for(page.imageUrl) %>" alt="<%= page.title %>"  class="header img-fluid">  
			 <% } else { %>	
				 <img src="<%= url_for(page.imageUrl) %>" alt="<%= page.title %>"  class="header img-fluid">  
				 <!-- We show this image on mobile and desktop only -->
				 <img src="<%= url_for(page.thumbnailUrl) %>" alt="<%= page.title %>"  class="header img-fluid header-fallback">   
			 <% } %>	

		</div> 			
		<div class="intro col-12 offset-0 col-lg-5 pl-0 pl-lg-4 pl-xl-5 pt-3 pt-lg-3 pt-xl-5 ">
			  <% if (page.title){ %>
			    <h1><a href="<%= page.permalink %>"><%= page.title %></a></h1>
			  <% } %>
	      <% if (page.subTitle){ %>
	        <h2 class="post-preview-subtitle"><%= page.subTitle %></h2>
	      <% } %>
			  <% if (page.intro){ %>
				  	<%- markdown(page.intro) %>
			  <% } %>
			  <% if (page.vimeoId > 1){ %>
			  	<!-- Hide  on screens smaller than lg -->
				  <a href="#" class="btn-play btn d-none d-lg-inline-block" title="Play the video">Play video</a>
			  <% } %>			  
		</div> 			
  </header>
  
  <div class="row content">
  	<div class="col-12 pt-2 pt-lg-4">
		  <%- page.content %>  		
  	</div>
  </div>

</section>

