<article id="<%= page.layout %>" class="container-fluid" >
	<div class="row article article-type-<%= page.layout %>">
		<div class="col-12 col-md-6 ">
		  <% if (page.title){ %>
		    <h1 class="d-none"><%= page.title %></h1>
		  <% } %>
		  <%- page.content %>
		</div>
	</div>
</article>
